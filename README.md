Overall Layout

Uses Flexbox and rem-based layout. You can also add input fields as needed.

Logic Implementation

The effect is achieved by dynamically adding class names to the entire container box, which triggers the styles already defined in CSS.